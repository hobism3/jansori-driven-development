# Jansori Plugin — 개발 명세

> 상태: 개발 착수용 명세. 구현 완료 보고서가 아니다.
> 목표: 무엇을 만들고 무엇을 실패로 볼지 확정한다. 이미 확정된 보장과 아직 정하지 않은 항목을 구분해 남긴다.

## 0. 관련 문서

| 문서 | 역할 |
|---|---|
| 이 문서 | 제품 의도·불변조건·채택된 기준선·성공/실패 조건 |
| `docs/spec-reference.md` | Capsule JSON 구조, 주입 텍스트, 요청·응답 등 자료 형식의 예시 모음 |
| `docs/golden-data.md`, `docs/demo-scenario.md` | 실제 도메인 규칙과 시연 대본 |

`docs/spec-reference.md`의 예시는 형식을 보여주기 위한 것이며, 그 자체가 확정된 스키마나 API 계약은 아니다. 실제 계약은 §8에서 정한다.
제출 저장소의 진입점은 완성된 `README.md`다.

## 1. 문제와 핵심 명제

팀의 문제는 여러 개발 조직의 SystemC C-model 코드 스타일과 개발 규칙이 일관되지 않고, 반복되는 교정이 다음 작업자에게 전달되지 않는다는 것이다. 세부 IP 알고리즘 설계 조직이 여러 지역으로 나뉘어 있어 통합 이후에도 공통 개발 룰이 자리 잡지 못했다.

**C-model 룰은 양이 많다.** 그래서 부모 Capsule 하나에 세부 주제를 자식으로 두는 구조가 예외가 아니라 메인 시나리오다. R-03과 I-03이 여기서 나온다.

Plugin 설치자는 누구나 스킬의 생성자이자 소비자다. A·B·C는 고정 역할이나 인증 사용자 구분이 아니라 시연상의 구분이다.

> 한 사람의 잔소리가 Capsule에 쌓여 스킬을 진화시키고, 그 진화가 다음 사용자에게 자동으로 전달된다.

교정 저장이나 주입 성공만으로 충분하지 않다. 다음 사용자의 실제 작업 결과가 달라졌음을 증명해야 한다.

## 2. 실행 환경

| 항목 | 기준 |
|---|---|
| 제품 | Claude Code Plugin + 로컬 Python 서버 |
| Claude Code | 최소 v2.1.163. 필요한 기능·호출 설정은 설치 버전에서 확인하고 README에 실제 버전 기록 |
| 서버 | 개발 컴퓨터 한 대의 로컬 단일 인스턴스. 참여자가 각자 설치하지 않는다 |
| 시연 | A·B·C를 분리된 세션으로 재현. 세션 간 작업물은 공유하지 않는다 |

## 3. 반드시 재현할 A → B → C 흐름

1. A가 Plugin을 설치하고 업무를 시작한다.
2. Plugin이 서버의 스킬 인덱스를 전달하고 Claude가 관련 스킬 유무를 판단한다.
3. 관련 스킬이 없으면 업무를 수행하고, 완료 후 Claude가 저장을 제안한다.
4. A가 동의하면 유사 스킬을 재확인한 뒤 새 Capsule을 생성·등록한다.
5. B가 Plugin이 있는 새 세션에서 같은 종류의 업무를 시작한다.
6. B의 Claude가 적합한 Capsule을 골라 로드하고 규칙을 적용한다.
7. B가 작업 중 필요한 교정을 잔소리로 전달한다.
8. 대상과 공유 의도가 명확하면 반영하고, 추론·추가 확인이 필요하면 확인 후 반영한다. 임계값에 도달하면 compaction을 시작한다.
9. C가 새 세션과 깨끗한 작업물에서 같은 종류의 업무를 시작한다.
10. C가 갱신된 Capsule을 받아 교정을 적용한 결과를 만들고, 기대 결과와 비교한다.

응답 종료를 업무 완료로 단정하지 않는다. 미완성 스킬을 동의 전에 등록하거나, 저장·거절한 내용을 변경 없이 반복 제안하지 않는다.

## 4. 불변조건과 채택된 설계 기준선

### 4A. 불변조건 — 승인된 기능 범위 안에서 변경 불가

설계 대안이나 시간 부족을 이유로 아래 보장을 약화하지 않는다. 기능 자체의 축소는 §10의 승인·기준 갱신 절차를 따른다.
각 항목은 **규칙 — 막으려는 실패**이며, ID는 §11 실패 판정과 직접 대응한다.

| ID | 불변조건 |
|---|---|
| I-01 | **내용 버전은 불변이다.** 내용 변경은 새 version을 만들고 과거 버전을 보존한다 — 같은 `skill_id@version`이 다른 내용을 가리키면 사용자가 무엇을 받았는지 증명할 수 없다. |
| I-02 | **정상 조회에 성공하면 조회 시점의 최신 확정본을 전달하고, 실제 로드한 부모·자식 ID와 version을 기록한다** — 부모 버전이 같아도 자식은 바뀔 수 있어 부모 번호만으로 전달 내용을 식별할 수 없다. 서버 장애 시에는 I-07·I-08을 적용하며, 갱신본을 받았다고 표시하지 않는다. 로드 이후 다른 세션의 교정·compaction으로 새 버전이 생긴 것은 기존 로드의 위반이 아니다. |
| I-03 | **부모·자식 모두 body와 corrections를 반영한다** — 자식 nag로 서버 버전만 오르고 부모 로드가 옛 자식 body만 전달하면, 저장은 성공하지만 교정 전파는 실패한다. |
| I-04 | **오래된 변경안·compaction 결과가 최신 내용을 덮어쓰지 않는다** — v3 변경안 승인 대기 중 nag로 v4가 생길 수 있으며, `confirmed`는 허용 여부이지 변경안의 현재 유효성을 뜻하지 않는다. |
| I-05 | **재전송은 같은 교정·version을 중복 생성하지 않는다** — 첫 응답을 못 받은 재시도가 새로운 변경으로 처리되면 사용자의 한 번의 의도가 여러 번 반영된다. |
| I-06 | **같은 skill_id의 신규 중복 등록은 차단한다** — 하나의 식별자로 두 스킬이 생기면 교정과 다음 소비자의 선택이 갈라진다. 의미상 중복은 별도의 완화 대상이다. |
| I-07 | **서버 장애와 스킬 부재를 구분한다.** 장애 시 본 업무는 계속하되 존재 여부를 확인하지 못한 신규 등록은 하지 않는다 — 조회 실패를 '없음'으로 오인하면 중복 Capsule이 생긴다. |
| I-08 | **훅 대기에는 상한이 있고 장애가 본 작업을 막지 않는다** — 훅 안에서 LLM을 호출하지 않아도 서버 조회가 오래 걸리면 사용자 입력은 묶인다. |
| I-09 | **길이 때문에 유효 지침을 잃지 않는다** — 오래됨은 무효가 아니므로 오래된 교정부터 자르면 안 되며, 다음 nag 때만 정리하면 nag가 없는 동안 문제는 해결되지 않는다. |
| I-10 | **compaction 중에도 소비자는 현재 body와 corrections로 바로 업무를 진행한다** — 폴링·대기·재조회가 필요하면 본 업무가 정리 작업에 종속되며, 진행 상태 때문에 내용까지 달라져서도 안 된다. |
| I-11 | **compaction은 유효 지침과 assets·refs를 보존하고 실패 시 내용을 바꾸지 않는다** — 본문을 정리하다 기존 규칙을 잃으면 진화가 아니라 정보 손실이다. |
| I-12 | **새 version·최신 포인터·재전송 기록의 쓰기는 원자적이다** — 다중 사용자 지원을 제외해도 한 세션의 nag와 백그라운드 compaction이 겹쳐 불완전한 저장이 생길 수 있다. |
| I-13 | **교정은 올바른 대상과 공유 의도에 따라 반영한다** — 부모·자식 대상을 추론하거나 공유 의도가 불명확한데 확인 없이 쓰면 다른 스킬이나 원치 않는 공유 내용이 바뀐다. |
| I-14 | **보호 대상 변경은 승인 없이 통과하지 않는다** — 메타데이터·assets·refs를 임의 변경하면 발동 조건과 실행 자료가 바뀐다. 서버 플래그 검사와 실제 사람 동의의 증거는 구분한다. |
| I-15 | **부모·자식 관계의 깊이는 최대 1단계다.**  — 자식 Capsule은 다른 Capsule의 부모가 될 수 없고, 자식을 가진 Capsule을 다른 Capsule의 자식으로 연결할 수도 없다. 다단계 참조가 생기면 로드 범위와 교정 대상이 복잡해지고, 손자에 저장된 지침이 소비자에게 전달되지 않을 수 있다. 신규 등록과 refs 변경 모두 이 조건을 유지해야 한다. |

### 4B. 채택된 설계 결정 — 승인 후 변경 가능

아래는 이미 채택된 현재 기준선이며, 새로 결정해야 하는 빈칸이 아니다. 변경이 필요하면 §9를 따른다.

| 영역 | 현재 채택안 |
|---|---|
| 데이터·상태 | Capsule JSON 단일 원본, 소비 시 부모 SKILL.md를 로컬 원본으로 유지하지 않음. 진행 상태는 내용과 별도 관리 |
| 선택 | Claude가 인덱스의 name·description을 보고 선택. 훅 내부에서는 LLM 미호출 |
| 훅 역할 | UserPromptSubmit은 인덱스 전달, Stop은 저장 제안 안내. 훅은 직접 대화·저장하지 않음 |
| 호출 경로 | load를 모델 호출 가능한 Skill로 제공. `/jansori:save`·`/jansori:nag`와 명확한 자연어 요청은 공통 처리 경로 사용 |
| 버전·교정 순서 | 최초 v1, corrections는 오래된 순서로 누적. 해당 조건의 최신 명시적 교정 우선, 불명확하면 확인 |
| 보호 대상 | name·description·keywords·assets·refs 변경에 승인 플래그와 최신성 검증 요구 |
| 길이 초과 | 등록·nag·PATCH의 입구에서 렌더 길이 검사. 초과 시 기존 내용 보존·변경 거부·자식 분리 안내 |
| refs·활성 범위 | 참조 깊이 1단계. 새 업무는 새 세션, 한 세션은 부모 하나와 직접 자식만 활성화. 자식도 교정 가능 |
| compaction | corrections 개수 기준 기본 10·테스트 3. 별도 서브 에이전트에서 백그라운드 처리, 성공 시 새 version과 본문을 저장하고 corrections를 비움 |

길이 초과 처리에서 '오래된 항목부터 절단'과 '후속 nag가 올 때까지 방치'는 이미 검토해 기각한 대안이다. 오래됐다는 것은 무효라는 뜻이 아니고, 후자는 nag가 오지 않으면 영원히 정리되지 않기 때문이다. 동등한 보장과 승인 없이 다시 채택하지 않는다. refs를 축소하면 분리 안내의 대체 방안도 함께 갱신한다.

## 5. 보안 경계

서버는 인증 없는 로컬 시연용이다. 운영 서비스 수준의 보안을 갖췄다고 표현하지 않는다.

- `127.0.0.1` 또는 `::1`에만 바인딩한다. 사내망 전체에 노출하지 않는다.
- assets·refs의 실제 파일 저장 경로는 지정한 임시 디렉터리 밖으로 나갈 수 없어야 한다.
- 포함된 스크립트는 자동 실행하지 않는다. 내용을 요약해 보여주고 사용자의 명시적 동의를 받는다.
- 서버는 업무 소스코드와 프롬프트 원문을 자동 수집·저장하지 않는다. 사용자가 Capsule에 포함하는 경우와 구분한다.
- 비밀정보 패턴 검사는 보조 수단이다. 파일 경로 검사와 본문·스크립트 내용 검사를 혼동하지 않는다.
- Capsule은 참고 지침이며 현재 사용자의 요청과 실행 권한이 우선한다. 간접 프롬프트 주입의 잔여 위험을 공개한다.

## 6. 요구사항과 검증 구분

각 요구사항의 구체적 수용 기준은 아직 확정되지 않았다.

| ID | 요구사항 | 검증 |
|---|---|---|
| R-01 | 업무 시작 시 스킬 인덱스를 주입한다 | 자동 |
| R-02 | Claude가 인덱스에서 스킬을 선택해 로드한다 | 전사 |
| R-03 | 부모 로드 시 refs 자식을 body+corrections로 전개한다 | 자동 |
| R-04 | 로드된 스킬이 업무 수행에 반영된다 | 전사 |
| R-05 | 업무 완료 후 저장을 제안한다 | 전사 |
| R-06 | 신규 등록 전 유사 스킬을 재검색한다 | 자동: 수행 / 전사: 판단 |
| R-07 | 잔소리를 대상에 반영하고 version을 올린다 | 자동 |
| R-08 | 잔소리 대상이 부모·자식 중 올바르게 판정된다 | 전사 |
| R-09 | 임계값 판정과 base_version 기반 반영·거부가 동작한다 | 자동 |
| R-10 | 서브 에이전트 compaction이 기존 유효 지침을 유실시키지 않는다 | 전사 |
| R-11 | 보호 대상 변경은 승인 플래그 없이 반영되지 않는다 | 자동 |
| R-12 | 서버 장애가 사용자 업무를 차단하지 않는다 | 자동 |
| R-13 | 서버 장애가 중복 등록으로 이어지지 않는다 | 자동 |
| R-14 | 갱신된 스킬이 다음 사용자에게 전달된다 | 자동 + 전사 |

**자동**은 `make verify`가, **전사**는 `docs/demo-transcript.md`가 증명한다. 전사 항목도 기대 결과를 미리 정하면 결과물 자체는 기계적으로 확인할 수 있다. 기대와 실제가 없는 전사는 로그일 뿐 증거가 아니다.

§4A의 불변조건, §5의 보안 경계, 재전송 처리처럼 R-ID로 직접 표현되지 않는 필수 조건은 관련 요구사항의 수용 기준에 연결한다. 이를 빠뜨리거나, 반대로 형식적으로 R-ID를 늘리지 않는다.

## 7. 성공을 입증할 증거

`make verify`는 서버를 테스트 안에서 기동·종료하며 자격증명 없이 기계적 계약을 검증한다.
Claude의 선택·업무 적용·저장 제안·의미 보존은 실제 실행 전사와 사전에 정한 수용 기준으로 검증한다.

- C는 새 세션과 깨끗한 작업물에서 시작하며, C의 요청에 교정 문장을 직접 넣지 않는다.
- B가 수정한 파일이나 정답을 담은 명세·golden 문서도 C의 작업 환경에서 제외해 Capsule 외의 전달 경로를 통제한다.
- 로드한 부모·자식 ID와 version, 전달된 교정, 실행 명령, 생성 결과, 기대 결과와의 비교를 기록한다.
- 예: 기본 규칙은 멤버 변수 `m_`, B의 교정은 boolean에 `is_`, C의 준비 상태 변수는 `is_ready`여야 한다. 일반 멤버는 여전히 `m_`인지 함께 확인한다.
- 결과가 기대값과 같다는 사실만으로는 교정 없이 우연히 나왔을 가능성을 배제하지 못한다. **교정 전 / 교정 후·compaction 전 / compaction 후** 세 상태를 같은 초기 작업물과 같은 요청으로 비교하고, 각 실행에서 실제 로드한 version을 기록한다. 구체적 구성은 `docs/golden-data.md`와 `docs/demo-scenario.md`에서 확정한다.
- compaction을 승인된 축소로 제외했다면 세 번째 상태의 검증도 함께 제외하고, 정리 기능까지 검증했다고 표현하지 않는다.
- 자식에 잔소리 → 부모 재로드 → 자식 교정 전달을 별도로 검사한다. 부모 직접 교정만으로 대체하지 않는다.
- seed로 B부터 시연해도 A의 생성·동의·저장 경로는 별도 실제 전사로 증명한다.
- mock·저장·주입 성공을 실제 업무 성공으로 표시하지 않는다. 미검증·실패·미구현은 구분해 기록한다.

## 8. 아직 정하지 않은 것

아래는 이 명세가 의도적으로 비워 둔 항목이다. §4B의 결정을 다시 백지화하지 않고, 그 위에서 채운다.
**공통 세션 상태 계약 → 동작/API 매핑 → 오류·상한** 순으로 먼저 확정한다. 앞의 둘이 열려 있으면 주입 경로와 교정 경로를 나눠 만들 수 없다.

| 항목 | 정해야 할 내용 |
|---|---|
| 공통 세션 상태 | 활성 부모·자식 ID/version, 저장 수락·거절 상태의 저장 위치·수명·공유 방식 |
| 동작/API 매핑 | 모든 제품 동작의 요청·응답 매핑, 보호 변경·nag·compaction 계약, 구성요소 경계의 누락·충돌 |
| 오류·상한 | 훅 연결·응답 시간 상한값, 렌더·인덱스 크기 상한값, 잘못된 JSON·파일 실패 시 계속 진행할 범위 |
| 데이터·쓰기 | 저장 방식·스키마 검증·원자성, request_id 동일성·보존·승인 재요청, base_version 충돌 처리 |
| Plugin 실행 | 채택된 Skill의 구체 정의·모델 호출 허용·권한·서버 연결, save/nag 자연어 경로, Skill Generator |
| 업무 경계 | 새 업무와 후속 잔소리의 구분, 저장 재제안·재진입 방지 |
| refs 전개 | 누락·자기 참조·중복·손자 처리, 자식 파일 기록 위치와 경로 검증 |
| compaction 실행 | 서브 에이전트 호출·격리, 실패·중단 처리, base_version 충돌 시 재시작 |
| 실행 환경 | OS·Python·설치·재시작 후 보존 여부. 최소 버전·Stop additionalContext·훅 길이 전제를 실제 환경에서 확인 |

load가 실제로 호출되는지는 Skill 설정과 권한에서 확인한다. '안내 문자열 전달'과 '실제 모델 호출 성공'을 같은 것으로 취급하지 않는다.

## 9. 명세와 다른 방향이 필요할 때

- 이 명세가 요구사항·보안·실행 조건을 만족하고 차이가 선호나 확장에 그치면, 명세를 기본값으로 유지한다. 대안과 보류 이유를 짧게 기록한다.
- 불변조건 위반·보안 문제·요구사항 모순·플랫폼 미지원이면 명세를 강행하지 않는다. 해당 작업을 멈추고 최소 수정안·영향·검증 방법을 제시해 승인받는다.
- 이 명세에 없는 요구가 필요해지면 관련 요구사항과 검증 기준의 영향 부분을 함께 갱신하고 승인받는다. 상세 설계라는 이유로 요구를 몰래 추가하지 않는다.

변경 기록에는 관련 R-ID, 무엇을 어떻게 바꾸는지, 이유, 사람의 실제 응답, 확정 문서와 검증 위치를 남긴다.
승인된 변경은 이 명세와 관련 요구사항·검증 기준에 반영한다. 기록 없이 새 설계가 기존 요구사항을 덮어쓰지 않는다.

## 10. 제외 범위와 축소 원칙

인증·권한·사용자 식별, 여러 부모 동시 로드, 벡터 검색·임베딩, 제품용 웹 UI·관리자 화면은 제외한다.
참조 깊이 2단계 이상, 삭제·비활성화, 모순 자동 탐지, 의미상 중복 완전 차단, 운영 배포·모니터링도 제외한다.
마켓플레이스 배포·자동 업데이트, 전체 다중 사용자 동시성, 인덱스 자동 정리, 성능·부하·확장성·가용성 최적화도 제외한다.
로컬 쓰기 원자성, §4A·§5의 비기능 조건, 설명용 HTML/PPT는 제외 대상이 아니다.

**반드시 유지:** 대상 판정 → 교정 저장 → 다음 세션 로드 → 실제 결과 반영, 그리고 **다른 참가자가 5분 안에 이 루프를 이해할 수 있는 설명물**. 5분은 팀이 정한 목표다. A의 생성 경로 증거도 §7에 따라 남긴다.

축소 순서는 **`make seed` 자동화 → assets 전개 → refs 구조 → compaction**이다. compaction은 제품 차별점이므로 마지막까지 유지한다.
**README·데모 영상·최소 설명용 HTML 또는 PPT는 이 축소 목록에 넣지 않는다.** 꾸밈·분량은 줄일 수 있지만 문제·교정·다음 결과·증거 연결은 남긴다.

**축소는 사람이 승인한 뒤 관련 요구사항·검증 기준·§4A 불변조건·§11 실패 판정을 함께 갱신한다.** 예를 들어 refs를 빼면 I-03·R-03이, compaction을 빼면 I-10·I-11·R-09·R-10과 §7의 세 번째 비교 상태가, assets 전개를 빼면 I-14의 assets 부분과 §5의 스크립트 조건이 적용 대상을 잃는다. 해당 항목을 함께 정리하되 교정 전파 자체는 유지한다.
제외 기능은 '승인된 제외'로 표시하며 완료로 표시하지 않는다. 허위 기록·보안 위반은 기능 축소를 이유로 면제되지 않는다.
핵심 루프에 필요 없는 확장은 `Deferred. Not required to validate the v1 correction-propagation loop.`로 기록한다.

## 11. 실패 판정

아래 중 하나라도 남으면 완료로 표시하지 않는다.

| # | 판정 |
|---|---|
| F-01 | §4A의 불변조건(I-01~I-14) 중 하나라도 위반된 상태로 남아 있다 |
| F-02 | §5의 보안 경계 — 로컬 노출, 파일 경로, 스크립트 자동 실행, 자동 수집 — 를 위반한다 |
| F-03 | 깨끗한 C 환경과 실제 결과 비교 없이 교정 전파 성공을 주장한다 |
| F-04 | 실제로 하지 않은 검토·동의·검증을 수행한 것으로 기록한다 |
| F-05 | 미구현 기능이 완료로 표시되어 있다 |
| F-06 | 저장소만 읽고는 무엇이 동작하는지 알 수 없다 |
| F-07 | §12의 필수 설명·제출물이 없거나, 설명 이해도 확인을 수행하지 않고 완료로 표시한다 |

**승인된 기능 축소에 해당하는 불변조건은 §10에 따라 §4A에서 갱신하며, F-01의 적용 대상도 그에 맞춰 좁아진다.** 제외 이유와 승인 위치를 남긴다. 증거 정직성(F-03~F-07)과 남은 범위의 성공 조건은 축소와 무관하게 유지한다.
R-ID 없는 코드가 있다는 사실만으로 실패로 보지 않는다.

## 12. 제출물과 완료 판단

설명 자료를 구현이 끝나고 남는 시간에만 만드는 부가 작업으로 두지 않는다.

| 산출물 | 완료 기준 |
|---|---|
| README 진입점 | 문제·핵심 루프·설치/실행/검증·동작 범위·미구현/한계·증거 위치를 연결. 처음 보는 사람이 어디서 시작할지 알 수 있음 |
| 데모 영상 | B의 교정과 C의 새 세션 결과가 연결되며 실제로 동작한 범위를 보여줌. A 경로의 별도 증거 위치도 안내 |
| 설명용 HTML 또는 PPT | 둘 중 하나 이상. 문제 → B의 교정 → C의 달라진 결과 → 기존 지침 유지 → 검증 증거를 설명. 제품 웹 UI 구현과는 별개 |
| 설명 이해도 확인 기록 | 프로젝트 설명을 듣지 않은 다른 참가자가 자료를 보고 5분 안에 '무엇을 왜 만들었고 교정이 어떻게 전파됐는지' 설명 가능한지 확인해 결과·혼동점을 `docs/demo-transcript.md`에 기록. 여력이 있으면 같은 참가자가 데모 환경에서 교정 한 번을 등록하며 대상·공유 여부·반영 결과를 이해하는지도 관찰해 함께 기록. 이는 설명 자료의 이해도 확인이며, 제품 사용성 전체를 검증했다고 표현하지 않는다 |
| 실행·검증 명령 | `make run`, `make verify`, 범용 `make seed`. seed 자동화의 승인된 축소는 재현 가능한 수동 등록 절차로 대체 |
| 도메인·실행 증거 | `docs/golden-data.md`, `docs/demo-scenario.md`, `docs/demo-transcript.md`. 기대/실제 결과·명령·버전·실패/미검증 구분 |

golden 데이터와 대본은 팀 작성 자료다. 없는 내용을 실제 도메인 규칙처럼 만들지 않으며, 데이터 비종속 구현은 진행한다. `make seed`는 `fixtures/seed/` 아래 JSON을 읽어 등록하는 범용 명령으로 만들고 내용은 나중에 채운다.
완료 선언은 채택된 요구사항·§12 산출물·§11 실패 판정을 실제 증거와 대조한 뒤 한다. 문서 작성 자체를 제품 검증으로 취급하지 않는다.
