# 이번 샘플 보완 내역

범위: 사용자가 제공한 `(3).zip`의 개발 착수용 명세·업무 샘플·평가 자료만 보완했다.
Jansori 서버/Plugin, AI-DLC 실행 결과, 서비스 테스트 어댑터, 완성된 C 상태 구현은 만들거나 넣지 않았다.

## 기준 문서와 보존

ZIP 내부 SPEC.md는 검토·사용하지 않았다. 별도 첨부 `SPEC (2)(1).md`를 루트 SPEC.md로 byte 그대로 치환했다.
`docs/spec-reference.md`, B/C 영상 연산 cpp, C 클래스 starter hpp, 입력 PPM, seed 세 개,
B의 모든 잔소리 프롬프트와 C-START 요청은 `(3).zip` 내용 그대로다.
기존 영상 golden W-01~36과 오류 5건도 그대로 유지했다. 출처/해시는 `SOURCE-PROVENANCE.json`에 있다.

**원문 내부 교차참조 주의:** 별도 첨부 명세의 §4A에는 I-15가 있지만 §11 F-01 괄호는 I-01~I-14로 적혀 있다.
원문을 조용히 수정하지 않았다. 이 입력의 검사 초안은 §4A의 I-15도 적용한다.
§8 계약 정리 시 이 표기 차이를 명시하고 명세 문구 정리는 팀 확인으로 남긴다.
손자 허용 여부를 새 미정 선택지로 되돌리거나 I-15를 검사에서 제외하지 않는다.

## 수정 사항

| 이전 빈틈 | 반영한 샘플 보완 |
|---|---|
| I-15를 조회 깊이만으로 해석할 여지 | DEV-03 수정, DEV-43~46 추가. 자식의 부모화·기존 부모의 자식화 refs 변경 거부, 신규 등록 거부, 허용 경계 |
| DoRun 반환값을 버려 빈 이미지를 놓침 | C의 수치 42+5 입력을 DoProcess/DoRun 각각 검사. 공개 probe도 반환 이미지 출력 |
| 기본 파라미터로만 Reset 관찰 | 비기본 Radius/StrengthQ8/GainQ8에서 Reset 전후 이미지 비교 |
| 다른 객체 생성 후 첫 객체를 안 봄 | 두 객체 생성·교차 실행·양방향 Reset의 독립성 관측 |
| Gain이 256의 배수뿐 | W-37~42 추가. 음수 분수값·다채널·경계·비포화 출력을 포함 |
| 후보가 수정한 관측기를 믿을 여지 | 평가자 보관 runner/probe/API 헤더 사용, 고정 파일·후보 hash 기록 |
| 사전 PASS 기록을 재실행하기 어려움 | 표준 라이브러리 Python 검사기와 명령, 실제 재검사 JSON |
| 시작 지시가 길고 보완점 누락 | 전체 시작 프롬프트 갱신, 복사해 사용할 짧은 프롬프트 추가 |

개발 수용 사례는 42→46개, 수치 정상 사례는 36→42개, 상태 관측은 8→24단계다.
API 응답 코드·포트·상한 수치 등 §8의 미정 계약은 대신 확정하지 않았다.
추가 개발 사례는 JSON 초안이며 실제 I-15 서버 검증을 수행한 결과가 아니다.

## 증거 구분

`PREPARATION-CHECKS.json`: 이 패키지의 실제 입력/빌드/수치 검사. C 상태는 EXPECTED_INCOMPLETE.
`EVALUATOR-REGRESSION-CHECKS.json`: 임시 오류 후보를 검사기가 거르는지 확인한 합성 시험 기록.
`docs/demo-transcript.md`: 실제 제품/사람과 실행 후 채울 양식. **NOT RUN 유지**.

새 도구가 성공해도 실제 교정 전파·compaction·A 생성·사람 승인까지 성공했다고 표시하지 않는다.
